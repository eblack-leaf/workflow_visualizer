// button is now a clickable that has a mesh and spawns a text/icon if necessary
// + position ties for moving button
// + color change on_click receive (animation that holds color change for on-press/on_release) / (until toggled back with toggle buttons)
